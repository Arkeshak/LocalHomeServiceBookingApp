<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.LocalHomeServiceBookingApp">

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.customer.CustomerHomeActivity" />
        <activity android:name=".ui.customer.CustomerDashboardActivity" />
        <activity android:name=".ui.customer.CustomerBookingsActivity" />
        <activity android:name=".ui.worker.WorkerHomeActivity" />
        <activity android:name=".ui.worker.WorkerDashboardActivity" />
        <activity android:name=".BookingDetailsActivity" />

    </application>

</manifest>
